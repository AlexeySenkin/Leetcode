/* Write your T-SQL query statement below */
select p.project_id,
    ROUND(SUM(experience_years)*1.0 / COUNT(experience_years),2) AS average_years
from project p
    left join Employee  e ON e.employee_id = p.employee_id
group by p.project_id
order by 1;