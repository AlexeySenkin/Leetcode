/* Write your T-SQL query statement below */
SELECT user_id, name, mail
FROM Users
WHERE
    mail like '%@leetcode.com'
    AND mail LIKE '[a-Z]%'
    AND (SUBSTRING(mail,1,LEN(mail) - LEN('@leetcode.com'))) NOT LIKE '%[^0-9a-Z_.-]%'